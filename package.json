{
  "name": "my-vite-project",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format:check": "prettier . --check",
    "format:write": "prettier . --write",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "rimraf": "^6.0.0",
    "vite": "^7.2.4"
  }
}
